import './styles.css'
import { buildThemeSite } from '../shared/ThemeKit'

const Site = buildThemeSite({
  themeClass: 'theme-exaggerated-minimal',
  eyebrow: 'Open to opportunities',
})

export default Site
